import { PortfolioClient } from "@/components/portfolio/portfolio-client";

export const metadata = {
  title: "Priority Queue — RazorReclaim",
};

export default function PortfolioPage() {
  return <PortfolioClient />;
}
