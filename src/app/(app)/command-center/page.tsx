import { DashboardClient } from "@/components/dashboard/dashboard-client";

export const metadata = {
  title: "Command Center — RazorReclaim",
};

export default function CommandCenterPage() {
  return <DashboardClient />;
}
